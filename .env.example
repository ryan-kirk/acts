# No environment variables are required for the current static frontend.
# Vite only exposes variables prefixed with VITE_ to client-side code.
# Never place private API keys, service credentials, or backend secrets here.

# Optional example of a safe public variable if the app needs one later:
VITE_APP_TITLE="Bible Time & Place Explorer"
